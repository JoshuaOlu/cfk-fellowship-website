source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "webrick", "~> 1.8"   # required for Ruby 3+

group :jekyll_plugins do
  # No plugins required — this site uses only Jekyll core features.
end
